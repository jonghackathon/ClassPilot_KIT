---
name: Supabase + Prisma 7 연동 설정
description: Supabase PostgreSQL + Prisma 7 연동 완료 상태, 핵심 설정 파일 및 해결된 이슈 요약
type: project
originSessionId: fb1796b0-0a9a-4238-80b0-0dc820054b80
---
## 완료 상태 (2026-04-12)

Supabase + Prisma 7 연동 완료. 31개 테이블 마이그레이션 완료. 시드 데이터 삽입 완료.

**Why:** 12_Supabase_연동 태스크 작업 중 Prisma 7 breaking changes로 인해 여러 수정 필요.
**How to apply:** 아래 설정이 현재 적용 상태임. 변경 전 반드시 확인.

---

## .env 핵심 설정

```env
# 런타임 (Transaction Pooler, pgbouncer=true)
DATABASE_URL="postgresql://postgres:...@db.pwjjwbpqgiaxbhrpwaxo.supabase.co:6543/postgres?pgbouncer=true"

# 마이그레이션 전용 (Session Pooler, IPv4 호환)
DIRECT_URL="postgresql://postgres.pwjjwbpqgiaxbhrpwaxo:...@aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres"
```

- Direct URL (5432)은 IPv6 전용이라 연결 실패 → Session Pooler URL 사용
- Transaction Pooler (6543)은 마이그레이션 불가 → 마이그레이션은 DIRECT_URL 사용

---

## prisma/schema.prisma

```prisma
datasource db {
  provider = "postgresql"
  # url/directUrl 없음 — Prisma 7 breaking change
}
```

## prisma.config.ts

```typescript
import "dotenv/config";
import { defineConfig } from "prisma/config";

export default defineConfig({
  schema: "prisma/schema.prisma",
  migrations: { path: "prisma/migrations" },
  datasource: { url: process.env["DIRECT_URL"] },  // 마이그레이션용
});
```

---

## src/lib/db.ts — Prisma 7 Driver Adapter 필수

Prisma 7은 `new PrismaClient()` 만으로 동작 안 함. 반드시 `adapter` 또는 `accelerateUrl` 필요.

```typescript
import { PrismaClient } from '@prisma/client'
import { PrismaPg } from '@prisma/adapter-pg'
import { Pool } from 'pg'

function createPrismaClient() {
  const pool = new Pool({ connectionString: process.env.DATABASE_URL })
  const adapter = new PrismaPg(pool)
  return new PrismaClient({ adapter })
}

const globalForPrisma = globalThis as unknown as { prisma: PrismaClient | undefined }
export const prisma = globalForPrisma.prisma ?? createPrismaClient()
if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma
```

## prisma/seed.js — 어댑터 적용 필요

```javascript
require("dotenv/config")
const { PrismaClient } = require("@prisma/client")
const { PrismaPg } = require("@prisma/adapter-pg")
const { Pool } = require("pg")

const pool = new Pool({ connectionString: process.env.DATABASE_URL })
const adapter = new PrismaPg(pool)
const prisma = new PrismaClient({ adapter })
```

시드 실행: `node prisma/seed.js` (npx prisma db seed 아님 — prisma.config.ts에 seed 설정 없음)

---

## 설치된 추가 패키지

```bash
npm install @prisma/adapter-pg pg @types/pg
```
