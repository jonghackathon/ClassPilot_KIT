# 컴포넌트 명세

**연관 문서:** 04_와이어프레임_공통_컴포넌트.md / 01_설계_원칙_및_디자인_시스템.md
**대상:** 프론트엔드 (구현 인터페이스)

---

## 1. 파일 구조

```
components/
├── layout/
│   ├── AdminSidebar.tsx              ← 운영자 사이드바
│   ├── TeacherTabNav.tsx             ← 강사 탑 탭 네비
│   ├── StudentBottomTab.tsx          ← 수강생 바텀 탭
│   ├── TopBar.tsx                    ← 공통 상단바 (로고, 알림, 프로필)
│   ├── MiniTopBar.tsx                ← 수강생용 간소 상단바
│   └── MobileDrawer.tsx              ← 모바일 사이드바 오버레이
│
├── ui/
│   ├── StatusBadge.tsx               ← 상태 배지 (출결/과제/이탈/납부)
│   ├── StatCard.tsx                  ← KPI 지표 카드
│   ├── DataTable.tsx                 ← 데이터 테이블 (검색/필터/정렬/페이지네이션)
│   ├── EmptyState.tsx                ← 빈 상태 안내
│   ├── LoadingSkeleton.tsx           ← 스켈레톤 UI
│   ├── Modal.tsx                     ← 공통 모달 (폼/확인/뷰어)
│   ├── ConfirmDialog.tsx             ← 확인 다이얼로그
│   ├── Toast.tsx                     ← 알림 토스트
│   ├── ProgressBar.tsx               ← 프로그레스 바
│   ├── NotificationBell.tsx          ← 알림 벨 + 드롭다운
│   └── SearchFilter.tsx              ← 검색 + 필터 조합
│
├── attendance/
│   ├── AttendanceRow.tsx             ← 학생 1명 출결 입력 행
│   ├── AttendanceBulkButton.tsx      ← 전체출석 버튼 + 확인 모달
│   ├── AttendanceCalendar.tsx        ← 학생용 월별 달력
│   ├── AttendanceStats.tsx           ← 출석률 통계 (바+숫자)
│   └── AttendanceDaySelector.tsx     ← 요일/주 선택 UI
│
├── assignment/
│   ├── AssignmentCard.tsx            ← 과제 카드 (제출률 바, 마감일)
│   ├── SubmissionList.tsx            ← 제출 현황 테이블
│   ├── SubmissionHistoryModal.tsx    ← 작성 이력 타임라인 모달
│   ├── AssignmentEditor.tsx          ← 학생 과제 작성 + 자동저장
│   ├── AssignmentCreateModal.tsx     ← 과제 등록 모달
│   └── FeedbackModal.tsx             ← 피드백 작성 모달
│
├── copilot/
│   ├── CopilotResponseCard.tsx       ← AI 응답 카드 (타입별 색상)
│   ├── CopilotCardGrid.tsx           ← 4개 카드 그리드 (반응형)
│   ├── CopilotQuestionInput.tsx      ← 텍스트 + 음성 입력
│   ├── CopilotHistory.tsx            ← 이전 질문 목록
│   └── CopilotSessionInfo.tsx        ← 세션 정보 (반, 주제, 구성)
│
├── churn/
│   ├── ChurnRiskCard.tsx             ← 이탈 위험 학생 카드 (확장형)
│   ├── ChurnScoreBar.tsx             ← 위험도 바 (색상 그라데이션)
│   ├── ChurnFactorList.tsx           ← 위험 요인 목록 (개별 바)
│   ├── ChurnSummary.tsx              ← 위험/주의/정상 카운트 요약
│   └── ContactModal.tsx              ← 연락 기록 모달
│
├── review/
│   ├── ReviewSummary.tsx             ← 핵심 요약 카드
│   ├── ReviewQuiz.tsx                ← 복습 퀴즈 1문제
│   ├── ReviewQuizList.tsx            ← 퀴즈 목록 (진행률)
│   └── ReviewPreview.tsx             ← 다음 수업 예습 미리보기
│
├── bot/
│   ├── BotQuestionCard.tsx           ← 질문+응답 카드
│   ├── BotAnswerInput.tsx            ← 강사 추가 답변 입력
│   ├── BotFAQList.tsx                ← FAQ 목록
│   └── StudentQuestionForm.tsx       ← 학생 질문 입력 폼
│
├── recording/
│   ├── RecordingUploader.tsx         ← 파일 업로드 (드래그&드롭)
│   ├── RecordingProgress.tsx         ← 변환 진행 상태
│   ├── RecordingSummaryView.tsx      ← 요약 결과 표시
│   └── RecordingList.tsx             ← 녹음 정리 내역 목록
│
├── schedule/
│   ├── WeeklyGrid.tsx                ← 주간 시간표 그리드
│   ├── DailyList.tsx                 ← 모바일용 일별 리스트
│   ├── ScheduleBlock.tsx             ← 수업 블록 (반, 강사, 인원)
│   └── ConflictWarning.tsx           ← 시간 충돌 경고
│
├── payment/
│   ├── PaymentTable.tsx              ← 수강료 테이블
│   ├── PaymentSummary.tsx            ← 요약 (총액/납부/미납)
│   └── PaymentProcessModal.tsx       ← 납부 처리 모달
│
└── complaint/
    ├── ComplaintCard.tsx             ← 민원 카드 (AI 초안 포함)
    ├── ComplaintResponseModal.tsx    ← 민원 응답 작성 모달
    └── ComplaintStatusBadge.tsx      ← 민원 상태 배지
```

---

## 2. 주요 컴포넌트 Props 정의

### 2.1 레이아웃

#### TopBar

```typescript
interface TopBarProps {
  title?: string                    // 페이지 제목 (모바일에서 표시)
  showHamburger?: boolean           // 운영자 모바일 햄버거 메뉴
  onHamburgerClick?: () => void
  notificationCount: number
  user: {
    name: string
    email: string
    role: 'ADMIN' | 'TEACHER' | 'STUDENT'
  }
}
```

#### AdminSidebar

```typescript
interface AdminSidebarProps {
  currentPath: string               // 활성 메뉴 하이라이트
  churnDangerCount: number          // 이탈 예측 배지
  unprocessedComplaintCount: number // 민원 배지
  isOpen: boolean                   // 모바일 오버레이 상태
  onClose: () => void               // 모바일 오버레이 닫기
}
```

### 2.2 UI 컴포넌트

#### StatCard

```typescript
interface StatCardProps {
  label: string                     // "전체 수강생"
  value: number | string            // 87 또는 "87명"
  subtitle?: string                 // "활성 85명"
  icon?: React.ReactNode            // <GraduationCap />
  trend?: {
    direction: 'up' | 'down' | 'neutral'
    value: string                   // "+5", "-3"
  }
  variant?: 'default' | 'danger' | 'warning' | 'success'
  onClick?: () => void              // 클릭 시 드릴다운
  isLoading?: boolean               // 스켈레톤 표시
}
```

#### StatusBadge

```typescript
type StatusType =
  | 'PRESENT' | 'LATE' | 'EARLY_LEAVE' | 'ABSENT'     // 출결
  | 'SUBMITTED' | 'NOT_SUBMITTED' | 'LATE_SUBMIT'      // 과제
  | 'SAFE' | 'WARNING' | 'DANGER'                      // 이탈
  | 'PAID' | 'UNPAID' | 'PARTIAL'                      // 납부

interface StatusBadgeProps {
  status: StatusType
  size?: 'sm' | 'md'               // sm: 테이블용, md: 카드용
}
```

#### DataTable

```typescript
interface Column<T> {
  key: keyof T
  label: string
  sortable?: boolean
  width?: string                    // "120px", "20%"
  render?: (value: T[keyof T], row: T) => React.ReactNode
  hideOnMobile?: boolean            // 모바일에서 숨김
}

interface Filter {
  key: string
  label: string
  options: { value: string; label: string }[]
}

interface DataTableProps<T> {
  data: T[]
  columns: Column<T>[]
  searchPlaceholder?: string
  onSearch?: (query: string) => void
  filters?: Filter[]
  onFilterChange?: (filters: Record<string, string>) => void
  sortConfig?: { key: string; direction: 'asc' | 'desc' } | null
  onSort?: (key: string) => void
  pagination?: {
    page: number
    pageSize: number
    total: number
    onPageChange: (page: number) => void
  }
  onRowClick?: (row: T) => void
  emptyState?: {
    icon: React.ReactNode
    message: string
    description?: string
    action?: { label: string; onClick: () => void }
  }
  isLoading?: boolean
  headerAction?: React.ReactNode    // [+ 추가] 버튼 등
}
```

#### Modal

```typescript
interface ModalProps {
  isOpen: boolean
  onClose: () => void
  title: string
  size?: 'sm' | 'md' | 'lg'
  type?: 'form' | 'confirm' | 'viewer'
  children: React.ReactNode
  footer?: React.ReactNode
  preventClose?: boolean            // 변경사항 있을 때 닫기 방지
  isDanger?: boolean                // 위험 액션 모달 (확인 버튼 빨간색)
}
```

#### ConfirmDialog

```typescript
interface ConfirmDialogProps {
  isOpen: boolean
  onClose: () => void
  onConfirm: () => void
  title: string
  description: string
  confirmLabel?: string             // 기본: "확인"
  cancelLabel?: string              // 기본: "취소"
  isDanger?: boolean                // true → 확인 버튼 빨간색
  isLoading?: boolean               // 확인 버튼 로딩 상태
}
```

#### ProgressBar

```typescript
interface ProgressBarProps {
  value: number                     // 0~100
  max?: number                      // 기본: 100
  variant?: 'default' | 'success' | 'warning' | 'danger' | 'auto'
  // auto: 60+→green, 40~59→amber, ~39→red
  showLabel?: boolean               // "80%" 텍스트 표시
  size?: 'sm' | 'md'               // sm: h-1.5, md: h-2
}
```

### 2.3 출결 컴포넌트

#### AttendanceRow

```typescript
type AttendanceStatus = 'PRESENT' | 'LATE' | 'EARLY_LEAVE' | 'ABSENT'

interface AttendanceRowProps {
  student: {
    id: string
    name: string
    grade?: string
  }
  currentStatus: AttendanceStatus | null
  homeworkStatus: 'SUBMITTED' | 'NOT_SUBMITTED' | null
  onStatusChange: (status: AttendanceStatus | null) => void
  onHomeworkChange: (submitted: boolean) => void
  isLoading?: boolean
  disabled?: boolean                // 과거 날짜 + 권한 없음
}
```

#### AttendanceCalendar

```typescript
interface AttendanceDayData {
  date: string                      // "2026-04-07"
  status: AttendanceStatus | null
}

interface AttendanceCalendarProps {
  year: number
  month: number                     // 1~12
  data: AttendanceDayData[]
  onMonthChange: (year: number, month: number) => void
  onDateClick?: (date: string) => void
}
```

### 2.4 과제 컴포넌트

#### AssignmentEditor

```typescript
interface AssignmentEditorProps {
  assignmentId: string
  assignmentTitle: string
  assignmentContent: string         // 과제 설명
  dueDate: string                   // ISO date
  initialDraft?: string             // 기존 임시저장 내용
  onSubmit: (data: {
    content: string
    aiUsed: boolean
    aiUsageDetail?: string
  }) => void
  autoSaveInterval?: number         // ms, 기본: 300000 (5분)
}
```

#### SubmissionHistoryModal

```typescript
interface HistoryEntry {
  timestamp: string                 // ISO datetime
  charCount: number
  content: string
}

interface SubmissionHistoryModalProps {
  isOpen: boolean
  onClose: () => void
  studentName: string
  history: HistoryEntry[]
  aiUsed: boolean
  aiUsageDetail?: string
  submittedAt?: string
}
```

### 2.5 코파일럿 컴포넌트

#### CopilotResponseCard

```typescript
type CopilotCardType = 'beginner' | 'example' | 'advanced' | 'summary'

interface CopilotResponseCardProps {
  type: CopilotCardType
  content: string
  code?: string                     // type === 'example' 시 코드 블록
  isUsed: boolean
  isStreaming: boolean              // SSE 스트리밍 중
  onUse: () => void
  onCopy: () => void
}

// 타입별 색상 매핑
const cardColors: Record<CopilotCardType, { bg: string; icon: string; title: string }> = {
  beginner: { bg: 'green-50',  icon: '🟢', title: '초보자 설명' },
  example:  { bg: 'blue-50',   icon: '🔵', title: '예시 코드' },
  advanced: { bg: 'purple-50', icon: '🟣', title: '심화 추가 질문' },
  summary:  { bg: 'yellow-50', icon: '🟡', title: '판서용 핵심 정리' },
}
```

#### CopilotQuestionInput

```typescript
interface CopilotQuestionInputProps {
  onSubmit: (question: string) => void
  isGenerating: boolean             // true → 입력 비활성
  placeholder?: string
  enableVoice?: boolean             // Web Speech API 지원 여부
}
```

### 2.6 이탈 예측 컴포넌트

#### ChurnRiskCard

```typescript
interface ChurnFactor {
  score: number                     // 0~100
  detail: string                    // "연속 결석 2회"
}

interface ChurnRiskCardProps {
  student: {
    id: string
    name: string
    className: string
    classId: string
  }
  score: number                     // 0~100
  level: 'SAFE' | 'WARNING' | 'DANGER'
  factors: {
    attendance: ChurnFactor
    homework: ChurnFactor
    access: ChurnFactor
    questions: ChurnFactor
  }
  previousScore?: number            // 이전 위험도 (트렌드 표시)
  onContact?: () => void            // ADMIN만 (연락하기)
  onDetail: () => void              // 학생 상세 이동
  onChurn?: () => void              // ADMIN만 (이탈 처리)
}
```

### 2.7 복습 컴포넌트

#### ReviewQuiz

```typescript
interface ReviewQuizProps {
  questionNumber: number            // 1, 2, 3
  totalQuestions: number            // 3
  question: string
  options: string[]                 // 4개 선택지
  correctIndex: number              // 정답 인덱스 (0~3)
  explanation: string               // 정답 해설
  onAnswer: (selectedIndex: number) => void
  isAnswered: boolean
  selectedIndex?: number            // 선택한 답
  onNext?: () => void               // [다음 문제]
}
```

### 2.8 녹음 컴포넌트

#### RecordingUploader

```typescript
interface RecordingUploaderProps {
  onUpload: (file: File, lessonId: string) => void
  lessons: {
    id: string
    label: string                   // "(화-초4) 2026.04.08"
  }[]
  acceptedFormats: string[]         // [".mp3", ".m4a", ".wav"]
  maxSizeMB: number                 // 100
  isUploading: boolean
  uploadProgress: number            // 0~100
}
```

### 2.9 시간표 컴포넌트

#### WeeklyGrid

```typescript
interface ScheduleBlock {
  id: string
  className: string                 // "(화-초4)"
  classLabel: string                // "친절한 설명"
  teacherName: string
  studentCount: number
  dayOfWeek: number                 // 1(월)~6(토)
  startTime: string                 // "15:00"
  endTime: string                   // "17:00"
  hasConflict?: boolean
}

interface WeeklyGridProps {
  blocks: ScheduleBlock[]
  weekStart: string                 // ISO date (월요일)
  onBlockClick: (block: ScheduleBlock) => void
  onEmptyCellClick: (day: number, time: string) => void
  onWeekChange: (weekStart: string) => void
}
```

---

## 3. 공통 타입 정의

```typescript
// types/index.ts

export type UserRole = 'ADMIN' | 'TEACHER' | 'STUDENT'

export type AttendanceStatus = 'PRESENT' | 'LATE' | 'EARLY_LEAVE' | 'ABSENT'

export type SubmissionStatus = 'SUBMITTED' | 'NOT_SUBMITTED' | 'LATE_SUBMIT'

export type ChurnLevel = 'SAFE' | 'WARNING' | 'DANGER'

export type PaymentStatus = 'PAID' | 'UNPAID' | 'PARTIAL'

export type ComplaintStatus = 'PENDING' | 'IN_PROGRESS' | 'RESOLVED'

export type ConsultationType = 'PHONE' | 'TEXT' | 'IN_PERSON'

export interface PaginationParams {
  page: number
  pageSize: number
}

export interface PaginatedResponse<T> {
  data: T[]
  total: number
  page: number
  pageSize: number
  totalPages: number
}

export interface ApiResponse<T> {
  success: boolean
  data: T
  error?: {
    code: string
    message: string
  }
}
```
